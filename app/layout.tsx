import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "KANVI CTI PORTAL | Personal Cyber Threat Intelligence",
  description:
    "KANVI CTI PORTAL — Personal Cyber Threat Intelligence Workspace. Premium SOC dashboard for Kanvi.",
  icons: {
    icon: "/icon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="cyber-grid min-h-full bg-[#030712] text-slate-100">
        {children}
      </body>
    </html>
  );
}
